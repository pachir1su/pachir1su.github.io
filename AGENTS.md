# AGENTS.md

이 파일은 이 저장소에서 일하는 **모든 AI 도구의 공통·표준 지침**입니다.
Claude Code, ChatGPT, Codex 등 어떤 도구로 작업하든 이 규칙을 먼저 따릅니다.

도구별 진입점(`CLAUDE.md` 등)은 공통 정책을 복사하지 않고 이 문서를 참조합니다.
현재 이슈나 다음 배포처럼 금방 낡는 정보는 열린 GitHub 이슈와 최근 PR에서 읽습니다.

## 프로젝트

- 이 저장소는 이건영의 개인 포트폴리오 정적 사이트입니다.
- GitHub Pages에 순수 HTML, CSS, JavaScript로 배포합니다.
- 빌드에는 Node.js 내장 모듈만 사용하며 프레임워크나 패키지 설치를 추가하지 않습니다.
- 문서와 사용자 안내는 한국어가 기본입니다.
- 사이트 문구는 `data-en`/`data-ko` 또는 `data-en-html`/`data-ko-html`로 영어와
  한국어를 함께 제공합니다.

## 사이트 데이터와 생성 영역

- 프로젝트 카드 목록의 단일 소스는 `projects.json`입니다.
- `node build.js`는 `index.html`의 `BUILD:PROJECTS:START`와
  `BUILD:PROJECTS:END` 사이를 생성합니다. 이 영역은 직접 수정하지 않습니다.
- Hero, About, Skills 등 마커 밖 영역과 `projects/<name>/` 상세 페이지,
  `mindmap.html`은 직접 관리합니다.
- 프로젝트 상세 페이지가 추가되면 `projects.json`, `index.html` 생성 영역,
  `sitemap.xml`의 경로가 서로 일치해야 합니다.

## AI 역할 분담

이 저장소에서는 개발과 독립 검증을 같은 AI 세션에 맡기지 않습니다.

### Codex·Claude Code 등 코딩 에이전트

- 역할은 구현, 리팩터링, 검증 코드 작성, 문서 갱신, Draft PR 생성까지입니다.
- 자신이 만든 변경에 대해 Node 검증, 브라우저/E2E, Lighthouse, 접근성 검사 같은
  테스트·검증 명령을 실행하지 않습니다.
- 기존 검증 코드와 계약을 읽거나 검증 코드를 작성하는 것은 가능합니다.
- 완료 보고에는 변경 파일, 구현 내용, 가정, 남은 위험, 실행하지 않은 검증을
  정확히 적습니다.
- 독립 검증 없이 "테스트 통과", "배포 준비 완료", "머지 가능"이라고 판단하지 않습니다.
- Draft PR까지만 만들고 ChatGPT 검토 전에 merge하거나 배포 상태를 확정하지 않습니다.

### ChatGPT

- 역할은 코딩 에이전트가 만든 PR·ZIP·diff의 독립 검토, 검증 명령 실행,
  브라우저 회귀 확인, 배포·머지 판단입니다.
- 코딩 에이전트의 완료 보고를 증거로 간주하지 않고 실제 diff와 결과를 확인합니다.
- 제품 코드 구현은 기본 역할이 아닙니다. 사용자가 명시적으로 역할을 바꾼 경우만
  예외로 합니다.

## 작업 시작 전에 읽을 것

작업을 시작하기 전에 최소한 다음을 실제로 읽습니다.

```text
README.md
CLAUDE.md 등 현재 도구의 진입점
관련 issue 본문 + 댓글
최근 관련 PR
관련 데이터, 코드, 상세 페이지와 검증 도구
```

- 업로드된 ZIP의 설명만 믿지 않고 실제 파일을 읽습니다.
- 이슈 본문뿐 아니라 댓글도 모두 확인합니다.
- 닫힌 이슈는 사용자가 명시적으로 요청하지 않는 한 근거로 삼지 않습니다.
- PowerShell에서 한글 파일은 `Get-Content -Encoding UTF8`로 읽습니다.

## 사용자 요구 해석

- 사용자가 "예를 들어"라고 든 내용은 예시이며 같은 원인의 인접 항목도 확인합니다.
- 범위는 요청한 결함과 같은 원인·같은 패턴까지만 확장합니다.
- 실제 비밀값, 데이터 삭제, 비가역 작업, 서로 배타적인 제품 요구처럼 결과가 크게
  달라지는 선택만 구현 전에 질문합니다.
- 요구가 명확하고 되돌릴 수 있으면 저장소 근거에 따라 진행하고 결과를 보고합니다.

## 구현 계획 품질

구현 전 계획에는 최소한 다음을 포함합니다.

```text
정확한 파일
현재 문제
수정할 데이터 또는 control flow
생성 파일 관계
오류 분기
한·영 문구 계약
검증 계약
브라우저 QA 항목
```

자연어 요구를 반복하는 것만으로 계획을 대신하지 않습니다.

## 코드 변경

- 기존 구조와 네이밍을 우선하며 변수와 함수는 `camelCase`를 사용합니다.
- 코드 블록은 역할이 드러나는 주석을 유지하고 예외 경계를 명시합니다.
- 불필요한 리팩터링과 범위 밖 변경을 피합니다.
- 생성 영역과 원본 데이터를 중복 편집하지 않습니다.
- 정적 자산, `og-image.png`, `robots.txt`, `sitemap.xml`은 영향이 명확할 때만 수정합니다.
- 공개 저장소이므로 개발자 도구, 소스 보기, 우클릭, 드래그, 텍스트 선택을 차단하지 않습니다.

## 비밀과 개인정보

- 비밀값, 실제 토큰, 비밀번호, 개인 설정을 출력하거나 커밋하지 않습니다.
- 프로젝트 소개에는 공개 저장소나 사용자 제공 자료에서 확인한 공개 정보만 씁니다.
- 팀 프로젝트의 인물 정보는 기존 공개 표기보다 구체화하지 않습니다.

## Git 브랜치와 원자성

- Codex가 브랜치를 만들 때는 `Codex/kebab-description-N` 형식을 사용합니다.
- `N`은 기존 `Codex/` 브랜치의 최대 번호보다 1 크게 정합니다. 없으면 1입니다.
- 랜덤 문자열 브랜치를 만들지 않습니다.
- 모든 PR에서 변경 파일 수와 커밋 수를 정확히 같게 유지합니다.
- 커밋 하나는 정확히 파일 하나만 변경하고, 변경 파일 하나는 정확히 커밋 하나에만
  등장해야 합니다.
- 추가·수정·삭제·rename 파일과 생성 산출물도 각각 별도 커밋으로 취급합니다.
- 커밋 메시지는 `<type>: <short description>` 형식이며 `feat`, `fix`, `refactor`,
  `docs`, `chore` 중 적절한 type을 사용합니다.

## 이슈와 PR

- PR 댓글의 "ㅇㅇ"는 곧 머지하겠다는 뜻이며 추가 작업 지시가 아닙니다.
- PR 본문에 관련 이슈 번호와 구현 범위를 명확히 연결합니다.
- 구현 내용, 변경 파일, 데이터·호환성 영향, 남은 위험, ChatGPT 검증 체크리스트를
  PR 본문에 포함합니다.
- Draft PR까지만 만들고 자동 merge하지 않습니다.
- 사용자가 요청하지 않은 주기적 PR 재검증이나 셀프 체크인을 예약하지 않습니다.

## 독립 검증 계약

코딩 에이전트는 아래 명령을 실행하지 않고 ChatGPT 검증 체크리스트에 남깁니다.

```text
node -c build.js
node -c script.js
node build.js
node tools/verify-build.js
```

ChatGPT는 추가로 다음을 확인합니다.

- `projects.json` JSON 파싱
- `node build.js` 재실행의 idempotency
- `sitemap.xml` XML 파싱과 상세 페이지 경로 일치
- 데스크톱·모바일, 라이트·다크 모드, 한국어·영어 전환
- 프로젝트 카드와 상세 페이지 링크
- F12, 우클릭, 드래그, 텍스트 선택이 차단되지 않는지

검증하지 않은 범위는 반드시 "실행하지 않음"으로 표시합니다.

## 릴리스와 배포

- Git 태그와 GitHub Release를 만들지 않습니다.
- 자동 merge하지 않습니다.
- 이 저장소에 명시적 버전 기준 파일이 없으면 임의로 버전을 만들지 않습니다.
- GitHub Pages 배포 결과는 PR 검증과 merge 이후 별도로 판단합니다.
