# 디자인 컨셉 변경 계획 (v3.X.X)

- 작성일: 2026-08-16
- 관련 이슈: #114 (디자인 컨셉 변경 및 UIUX 업데이트), #106 (어색한 문장 정리)
- 관련 분석: PR #112 (정보 전달력 · 프로젝트 전수 감사), PR #113 (성능 감사)
- 상태: **구현 시작**. 2026-08-29 최종 제품 결정은 아래 0-A가 우선한다.

---

## 0-A. 2026-08-29 최종 제품 결정

초기 문서와 프로토타입은 v2를 우주 컨셉으로 완전히 교체하는 방향까지 검토했지만,
최종 제품 구조는 다음과 같이 확정한다. 아래 결정과 충돌하는 이 문서 후반의 초기 가정은
역사적 검토 기록으로만 남기며 구현 근거로 사용하지 않는다.

1. **v2는 루트(`/`)에서 동결·유지한다.** v2에는 `/universe/`로 이동하는 작은
   게이트웨이만 추가한다.
2. **v3는 `/universe/`의 독립 정적 애플리케이션으로 배포한다.** v2와 CSS·JavaScript를
   공유하지 않고 프로젝트 데이터만 루트 `projects.json`에서 읽는다.
3. **v3는 포트폴리오 카드 페이지처럼 만들지 않는다.** 사용자는 관측자가 되어 연도별
   별자리, 프로젝트 천체, 성장 궤적, 수상 기록과 미완성 구역을 이동한다.
4. **GitHub Rank가 성장 기록의 중심축이다.** `C → C+ → B- → B → B+ → A- → A`
   사이에 저장소로 확인되는 성장 사건을 보조 천체로 연결하고, 이후 소유자가 선택한
   기록을 데이터 파일에 추가할 수 있게 한다.
5. **수상 4개는 동등하다.** 크기·밝기·중심 거리·번호로 서열화하지 않는다. 이 문서의
   초기 `밝기 등급(1~4등성)` 제안은 폐기한다.
6. **사운드는 이번 기본 구현에 연결하지 않는다.** 자동 재생하지 않으며, 별도 점검에서
   승인된 후보와 트리거가 확정된 뒤 지연 로딩으로 추가한다.
7. **이스터에그는 공동 구현한다.** 최초 배포에는 여러 관측 구역을 방문하면 나타나는
   이름 없는 별 한 개와 확장 가능한 상태 구조만 둔다.

### 배포 구조

```text
/
├─ index.html                 # v2 + Universe 게이트웨이
├─ projects.json              # 프로젝트 단일 원본
└─ universe/
   ├─ index.html              # 관측 UI와 접근성 구조
   ├─ universe.css            # 라이트 성도 / 다크 우주 / 반응형
   ├─ universe.js             # 데이터 로딩·관측 상태·언어·테마·URL 상태
   └─ universe-data.json      # GitHub Rank 성장 궤적·수상 4개
```

GitHub Pages 저장소에 v2와 v3가 함께 있어도 서버 연산 부하는 생기지 않는다. 브라우저는
현재 방문한 경로의 리소스만 내려받으므로 `/universe/` 자산은 v3에 들어온 사용자에게만
전송된다. 성능 예산은 정적 CSS·SVG, 입력 시점 DOM 갱신, 사운드 미연결을 기본으로 한다.

---

## 0. 결정 사항

이슈 #114 댓글에는 서로 배타적인 두 방향이 있었다.

1. 우주 / 별자리로 컨셉 변경
2. 혹은 #112 · #113을 참고해 **노트 스타일을 유지하고 UIUX만** 변경

저장소 소유자 결정으로 **둘을 버전으로 나눈다.**

| 버전 | 범위 | 상태 |
|---|---|---|
| **v2.1.0** | 노트 컨셉 유지. 문구 · 접근성 · 런타임 성능 개선 | 구현 완료 (PR #115) |
| **v2.2.0** | 정보구조 재배치 · 대표작 압축 · 장식 번호 제거 | 구현 완료 (PR #116) |
| **v3.0.X** (이후) | 우주 · 별자리 시각 컨셉 변경. 이 문서가 그 준비 문서다 | 미착수 |

컨셉을 나중으로 미루는 근거는 취향이 아니라 감사 결과다. PR #112는
`paper / notebook 컨셉 전체`를 **Keep P0**으로 두고 이렇게 적었다.

> 실제 차별화 요소. 문제는 컨셉이 아니라 컨셉이 실어 나르는 내용
> … 컨셉 · 디자인은 손댈 필요가 없다.

즉 30초 테스트를 막고 있던 것은 컨셉이 아니라 정보 위계 · 증거 부재였다.
그쪽을 먼저 고쳐야 컨셉 교체의 효과를 따로 측정할 수 있다.

---

## 1. v2.1.0에서 이미 처리한 것

v3 작업을 시작할 때 **다시 하지 않아도 되는 항목**이다.

| 구분 | 내용 |
|---|---|
| 정보 위계 | Hero 고정 문구를 직군 · 산출물 명사로 교체, 포부는 부제로 강등 |
| 정보 위계 | 카드마다 역할 한 줄(`role`) 노출 — 상세 페이지 표기를 그대로 사용 |
| 신뢰 | 자기평가 형용사(탁월한 · 능숙한 · 효과적인 · 多) 제거 |
| 신뢰 | 1인 개발 보일러플레이트 6곳 · `그 외 다수` 12곳 제거 |
| 신뢰 | 자기신고 진행률 %(0% · 5% 빈 막대) → 상태 라벨 |
| 동선 | 일반 프로젝트 카드 전체 클릭 가능, 터치 히트 영역 44px |
| 모바일 | 하단 네비 9 → 5, 480px 카드 좌측 마진선 44 → 32px |
| 성능 | Hero 스티커 rAF를 viewport · 탭 가시성 · reduced-motion 기준으로 중단 |
| 성능 | 769~1024px CSS/JS device 판정 불일치 해소(포인터 능력 기준) |
| 성능 | 필터 클릭당 handler 3개 · 카드 2회 순회 → 단일 handler |
| 성능 | progress bar `width` → `transform: scaleX()` |

### v2.2.0에서 처리한 것 (PR #116)

정보구조 항목이라 v3 TODO에서 내려간다.

| 구분 | 내용 |
|---|---|
| 정보구조 | 섹션 순서를 산출물 → 방법 → 사람으로 (`Home → Projects → Skills → Awards → Vision`) |
| 정보구조 | 대표 프로젝트 도달 거리 데스크톱 5.0 → 1.3 화면째, 모바일 7.2 → 1.1 화면째 |
| 대표작 | **5개 → 3개로 확정 적용** — koreatechGongjiAgent · MultiMind · github_rank_insight |
| 대표작 | `Wall_Sina` · `HEALTH_CHECK_PROJECT`는 대표에서 내리고 전체 목록에 유지 |
| 대표작 | `nonofire`는 수상 섹션에만 남김 (본인 기여가 CV 알고리즘이 아니므로) |
| About | **MBTI 5줄 막대 차트 제거** → 자기보고 한 줄(`.about-note`)로 축소 |
| About | GitHub 통계 SVG 3장 → 1장 (`streak-stats.demolab.com` 의존 해소) |
| 장식 | 수상 `01~04` 순번 · 프로젝트 카드 `p.01` 카운터 · 책갈피 하위 항목 4개 제거 |

---

## 2. v3에서 지켜야 하는 제약

컨셉이 바뀌어도 `AGENTS.md`의 제약은 그대로다.

```text
프레임워크 · 패키지 설치 없음 (Node 내장 모듈만)
순수 HTML / CSS / JavaScript, GitHub Pages 정적 배포
projects.json 단일 소스 + build.js 생성 구조 유지
data-en / data-ko 이중 언어 계약 유지
라이트 · 다크 모드 유지
개발자 도구 · 우클릭 · 드래그 · 텍스트 선택 차단 금지
```

추가로 v2.1.0이 만든 계약도 유지한다.

- `projects.json`의 `role` · `status` 필드
- `data-card-href` 카드 전체 클릭
- 포인터 능력 기준 device 판정 (`(hover: hover) and (pointer: fine)`)

---

## 3. 컨셉 후보

이슈 #114의 우주 / 별자리를 포함해 6개를 놓고 비교한다.
모두 이 저장소의 실제 콘텐츠(프로젝트 31개 · 수상 4개 · 실패 5개 · 이중 언어)를
담을 수 있는지를 기준으로 본다.

### A. 우주 · 별자리 (이슈 #114 원안)

- **축**: 프로젝트를 별로, 분야 연결을 별자리로. 연도는 궤도 · 성단.
- **강점**: 프로젝트 간 관계를 그림으로 말할 수 있다. `mindmap.html`이 이미 있어
  개념이 겹치고, 다크 모드가 기본값이 되어 자연스럽다.
- **약점**: 어두운 배경 + 작은 발광 텍스트는 본문 가독성과 대비를 잃기 쉽다.
  실패 섹션 · 진행 상태 같은 **부정 정보**를 별자리 은유로 표현하기 어렵다.
- **비용**: 높음. 배경 렌더링(캔버스 or SVG)이 새로 필요하고 성능 감사가 지적한
  `상시 rAF` 문제를 다시 만들 위험이 크다.
- **리스크**: 장식이 정보를 덮는 실패로 되돌아갈 수 있다(PR #112의 핵심 지적).

### B. 청사진 · 설계도 (Blueprint)

- **축**: 시안 청색 배경 + 흰 선, 치수선 · 부품 번호 · 도면 프레임.
- **강점**: 하드웨어 · 임베디드 프로젝트 비중이 높은 이 포트폴리오와 직접 맞는다.
  `문제 / 한 일 / 결과` 3단 계약을 도면 주기(annotation)로 표현할 수 있다.
- **약점**: 단색 계열이라 수상 카드의 금 · 은 · 동 구분이 약해진다.
- **비용**: 중간. 현재의 선 · 격자 자산을 상당 부분 재사용할 수 있다.

### C. 터미널 · CLI

- **축**: monospace 전면, 프롬프트 · 출력 블록 구조, 필터를 명령어로.
- **강점**: 개발자 독자에게 신호가 가장 직접적이다. 이미 `--font-mono`가 있다.
- **약점**: 한글 monospace 조판이 길어지면 읽기 어렵다. 이중 언어와 상성이 나쁘다.
  개발자가 아닌 독자(교수 · 대회 심사)에게 진입 장벽이 생긴다.
- **비용**: 중간.

### D. 실험 노트 · 아카이브 (현재 컨셉의 확장)

- **축**: 노트를 버리지 않고 **연구 노트 / 로그북**으로 좁힌다. 날짜 · 기간 ·
  관측 · 결과 칸이 컨셉 자체에 들어 있다.
- **강점**: PR #112가 지적한 **기간 · 결과 · 배운 점 부재**를 컨셉이 강제한다.
  기존 CSS 자산 재사용률이 가장 높고 회귀 위험이 가장 낮다.
- **약점**: "컨셉을 바꿨다"는 인상은 가장 약하다.
- **비용**: 낮음.

### E. 인쇄 편집 · 신문 조판 (Editorial)

- **축**: 그리드 · 큰 표제 · 컬럼. 장식 대신 타이포그래피로 위계를 만든다.
- **강점**: 정보 밀도가 높은 이 사이트(카드 31개)와 잘 맞는다. 장식 인터랙션
  9종을 자연스럽게 정리할 명분이 된다.
- **약점**: 개성이 약해질 수 있고 폰트 선택에 품질이 크게 좌우된다.
- **비용**: 중간~높음. 레이아웃 재설계가 핵심이라 CSS 재작성 범위가 넓다.

### F. 회로 기판 (PCB)

- **축**: 기판 녹색 · 금색 패드, 트레이스 선으로 섹션 연결.
- **강점**: 하드웨어 정체성이 강하다.
- **약점**: 소프트웨어 프로젝트(봇 · LLM 도구 · 웹)와 어긋난다. 실제 포트폴리오
  구성은 소프트웨어가 더 많다.
- **비용**: 높음.

### 1차 정리

| 후보 | 콘텐츠 적합 | 가독성 리스크 | 마이그레이션 비용 |
|---|---|---|---|
| A 우주 · 별자리 | 중 | **높음** | 높음 |
| B 청사진 | 높음 | 낮음 | 중 |
| C 터미널 | 중 | 중(한글 조판) | 중 |
| D 실험 노트 | 높음 | 낮음 | **낮음** |
| E 인쇄 편집 | 높음 | 낮음 | 중~높음 |
| F 회로 기판 | 낮음 | 중 | 높음 |

### 결정 — **A. 우주 · 별자리로 확정** (2026-08-17)

저장소 소유자 결정. 청사진·터미널 계열은 반려됐다. A는 위 표에서 콘텐츠 적합·비용이
최상위가 아니지만 소유자가 컨셉의 정체성을 우선했다. 따라서 아래 조건들이
가독성·성능 리스크를 상쇄하는 전제가 된다.

#### 실패·진행 상태 은유 — 별의 생애주기

컨셉 A의 최대 난제로 지적했던 "부정 정보를 별 은유로 어떻게 쓸 것인가"는
별의 생애주기를 쓰면 억지 없이 맞아떨어진다.

| 사이트 상태 | 천체 | 근거 |
|---|---|---|
| 완료 프로젝트 | 주계열성 | 안정적으로 빛나는 단계 |
| 진행 중 | 원시성(protostar) | 아직 점화 전 |
| **실패** | **갈색왜성** | **질량이 부족해 핵융합에 이르지 못한 천체** |
| 수상 4개 | 동등한 기록 천체 | 크기·밝기·중심 거리 차이 없이 같은 무게로 표현 |
| 카테고리 | 별자리 선 | 분야로 별을 잇는다 |
| 연도 그룹 | 관측 시기 | |

갈색왜성은 "시도했으나 점화하지 못한" 실제 천체라 자기비하 없이 실패를 표현한다.

수상 표현은 **v2.2.0에서 이미 제거된 `01~04` 순번을 되살리지 않는다.** 전국 8위,
특별상, 동상, 로컬임팩트상은 서로 성격이 다른 기록이므로 시각적 서열을 만들지 않고,
선택한 뒤에만 대회·프로젝트·역할의 차이를 글로 확인한다.

#### 구현 시 반드시 지킬 것

1. **배경은 정적 SVG + CSS만.** 캔버스 상시 rAF는 금지 — v2.1.0에서 없앤 문제(#113 P1-1)를
   되살리게 된다. `hero-grid-bg`·`hero-glow` 레이어 슬롯이 이미 있어 DOM 추가가 거의 없다.
2. **라이트 모드는 성도(星圖) 반전.** 흰 종이에 검은 별. 우주 컨셉의 라이트 모드는 대개
   실패하므로 "다크 = 밤하늘 / 라이트 = 인쇄된 성도"로 역할을 나눈다.
3. **기본/대체 관계 반전.** 현재 `:root`가 라이트(`#faf7ee`)이고 `[data-theme="dark"]`가
   오버라이드 18개다. 우주는 다크가 기본이라 이 구조를 뒤집어야 하며 18개가 재작성 대상이다.
4. **muted 계열 대비 재측정.** impeccable `gray-on-color` 안티패턴이 여기서 터진다.
   v2.1.0에서 만든 `--*-text` 토큰 방식(장식색과 텍스트색 분리)을 그대로 이어간다.

#### 현재 코드의 출발점 (실측)

우주 컨셉은 생각보다 유리하다 — **다크 팔레트가 이미 남색 계열이다.**

```css
[data-theme="dark"] { --paper: #1a1a2e; --paper-2: #16213e; --card: #1f2937; }
```

반대로 걸림돌은 `rgba(31, 28, 20, …)` 라이트 잉크색 **25곳 하드코딩**이다.
다크에서 따라오지 않으므로 Phase 1 토큰화가 선택이 아니라 전제다.

---

## 4. 참고 저장소에서 가져올 규칙

이슈 #114 댓글의 세 저장소에서 이 프로젝트에 바로 적용할 수 있는 항목만 옮긴다.

### `3x-haust/oh-my-design` — 결정 순서

컨셉을 먼저 그리지 않고 아래 순서를 지킨다.

```text
brief → evidence → copy → typography → composition → build once → critique
```

핵심은 **"레이아웃 전에 문구를 먼저 확정한다"**는 것이다. v2.1.0에서 문구를
정리한 것이 곧 이 단계에 해당하므로, v3은 composition부터 시작할 수 있다.

### `pbakaus/impeccable` — 피해야 할 패턴

- 과다 사용 폰트(Inter 등) 사용 금지
- 컬러 배경 위 회색 텍스트 금지 → **우주 컨셉(A)에서 특히 위험**
- 순수 검정 · 순수 회색 대신 톤을 넣을 것
- 카드 중첩 과다 금지 → `.about-card > .mbti-card` 중첩은 v2.2.0에서 해소됨.
  새 컨셉에서 카드 안에 카드를 다시 만들지 않는다
- bounce · elastic 이징 금지 → **v2.1.0에서 해소** (`--ease-out` 토큰으로 교체)

> 이 규칙들은 README 요약이 아니라 저장소의 detector 규칙(`scripts/detector/`, 59개)을
> 실제로 실행해 확인했다. v2.1.0 적용 후 149건 → 31건이며, 남은 31건은 노트 컨셉
> 자체(견출지 탭 · 종이 배경 · 노트 줄)라 v3 교체에서 사라진다.

### `emilkowalski/skills` — 모션

- 진입 애니메이션은 `ease-out` — `STANDARDS.md` 권장 커브
  `cubic-bezier(0.23, 1, 0.32, 1)`를 `--ease-out` 토큰으로 도입 완료
- 단색 테두리보다 반투명 그림자
- UI 애니메이션은 300ms 이하, `transform`·`opacity`만 애니메이트
- 빈도표: 하루 수십 회 발생하는 hover 모션은 "제거 또는 대폭 축소" 대상
- **모든 요소가 움직일 필요는 없다** → PR #112가 센 부수 인터랙션 9종과 직결

---

## 5. v3 진입 조건

컨셉 교체를 시작하기 전에 아래가 먼저 끝나 있어야 한다.

1. v2.1.0의 브라우저 회귀 검증 완료 (ChatGPT 독립 검증)
2. PR #112가 P0로 지적한 **증거 부재** 해소 — 상세 페이지 27개 중 스크린샷이
   1개뿐인 상태에서 컨셉을 바꾸면 같은 문제가 새 옷을 입고 남는다
3. 아래 편집 판단을 소유자가 확정 — 이들은 자동 적용하지 않는다
   - 디스코드 봇 3종(`MZ_bot` · `SH_bot` · `SG_bot`) 통합 여부
   - 한기대 지도 진행/실패 중복 카드 통합 여부
   - `Sibal_Life_Smoking/_archive.html` 노출 처리
   - Skills 숙련도 3단 구분(주력 / 사용 경험 / 학습 중) 등급 지정
   - GitHub Rank 사이에 추가할 개인 성장 기록과 공개 범위

> 대표 프로젝트 5 → 3 축소와 MBTI 블록 처리는 **v2.2.0에서 확정·적용**됐다.
> v3에서 다시 결정할 항목이 아니다.

---

## 6. 구현 이후 독립 검증 계약

`/universe/` 기본 구현은 시작됐지만 저장소의 AI 역할 분담에 따라 코딩 세션에서 아래
검증을 실행하지 않는다. **ChatGPT 독립 검증 대기** 상태로 인계한다.

```text
node -c universe/universe.js
projects.json · universe/universe-data.json JSON 파싱
sitemap.xml XML 파싱
데스크톱·모바일 / 라이트·다크 / 한국어·영어 브라우저 회귀
연도·성장·수상·미완성 구역과 프로젝트 상세 링크
키보드 탭 이동·화살표 구역 전환·Escape 패널 닫기
잘못된 해시와 데이터 로딩 실패 분기
```

초기 후보별 마이그레이션 비용은 `style.css` 구조를 읽고 추정한 값이며 실제 측정치가
아니다. 독립 검증 전에는 배포 준비 완료나 회귀 없음으로 판정하지 않는다.
