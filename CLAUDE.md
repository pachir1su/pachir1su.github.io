# CLAUDE.md

Claude Code 작업 지침입니다. 이 저장소(**이건영 포트폴리오**, `pachir1su.github.io`)에서
작업할 때는 아래 규칙을 우선 적용합니다.

## 프로젝트

- **개인 포트폴리오 정적 사이트**입니다. GitHub Pages로 배포되는 순수 HTML/CSS/JS이며,
  번들러·프레임워크 없이 **Node 내장 모듈만** 사용합니다(`.env`, `package.json` 없음).
- 프로젝트 카드 목록은 **`projects.json`이 단일 소스**입니다. `node build.js`가 이 데이터로
  `index.html`의 `BUILD:PROJECTS:START ~ END` 마커 사이를 생성합니다. **이 영역은 직접 수정하지
  말고**, `projects.json`을 고친 뒤 `node build.js`를 실행합니다.
- 마커 밖 영역(Hero·About·Skills 등)과 `projects/<name>/` 상세 페이지, `mindmap.html`은
  **수작업으로 관리**합니다.
- 문서·사용자 안내는 **한국어를 기본**으로 하되, 사이트에 노출되는 문구는 `data-en`/`data-ko`
  (또는 `data-en-html`/`data-ko-html`)로 **한국어·영어를 함께** 채웁니다.

## 브랜치 네이밍 규칙

브랜치 생성 시 반드시 아래 형식을 따를 것:

```
claude/kebab-description-N
```

| 항목 | 규칙 |
|---|---|
| `claude/` | 고정 prefix, 절대 생략 금지 |
| `kebab-description` | 2~5단어, 소문자, 하이픈 구분 |
| `-N` | 기존 `claude/` 브랜치 최대 번호 + 1 |

1. `git branch -a | grep 'claude/'` 로 기존 번호 확인
2. 최대 번호 + 1 을 N으로 사용 (없으면 N=1)
3. `git checkout -b claude/your-slug-N`
4. 랜덤 문자열(예: `claude/beautiful-hopper-FvyrU`) 절대 사용 금지

> 별도로 개발 브랜치가 지정된 세션에서는 지정 브랜치를 우선합니다.

## 작업 방식

- 이슈를 지정하면 해당 이슈 번호를 먼저 확인합니다. 본문뿐 아니라 댓글까지 모두 읽고 근거로 삼습니다.
- **구현 전 설계 방향을 먼저 확인**하고, 파일 수정 전 기존 코드를 파악한 뒤 진행합니다.
- 한 작업 안에서 커밋은 되도록 잘게, 많이 쪼갭니다(데이터 → 빌드 → 스타일 → 문서 등 단위별로).
- 큰 변경은 단계별로 나눠서 진행합니다.
- QA·검증할 항목이 있으면 PR 본문에 정리해 넣습니다(무엇을 어떻게 확인했는지, 남은 확인 사항 포함).
- 사용자가 직접 해야 할 일(설정 변경, 실기기·브라우저 확인 등)이 있으면 하나하나 구체적으로 알려줍니다.
- 궁금하거나 불확실한 점은 임의로 결정하지 말고 항상 먼저 물어봅니다.
- **PR을 주기적으로(예: 1시간마다) 자동 재검증하거나 셀프 체크인을 예약하지 않습니다.**
  사용자가 명시적으로 요청할 때만 합니다.

## 이슈와 PR

- **PR 댓글의 "ㅇㅇ"는 "곧 머지하겠다"는 뜻입니다.** 추가 작업을 시작하라는 지시가 아니므로,
  그걸 근거로 새 작업을 벌이지 않습니다. 다음 작업은 사용자가 명시적으로 요청할 때만 시작합니다.
- 닫힌 이슈는 사용자가 명시적으로 요청하지 않는 한 읽거나 근거로 삼지 않습니다.
- 이슈를 확인할 때는 본문뿐 아니라 댓글도 확인합니다.
- PR은 카테고리 단위로 나누고, 커밋은 이슈나 작업 단위로 작게 쪼갭니다.
- PR 본문에는 관련 이슈 번호를 명확히 연결합니다.
- 푸시 후 해당 브랜치에 열린 PR이 없으면 **draft PR**을 생성합니다.
- 불확실한 요구사항은 구현 전에 사용자에게 질문합니다.

## 코드 변경

- 기존 구조와 네이밍 스타일을 우선 따릅니다. 성능·가독성을 함께 챙기고, 변수·함수명은
  `camelCase`, 코드 블록마다 역할 주석을 답니다. 실행 시 예외 처리를 잊지 않습니다.
- 불필요한 리팩터링과 범위 밖 변경은 피합니다.
- 비밀값, 실제 토큰, 개인 설정 파일을 출력하거나 커밋하지 않습니다.
- 정적 자산(이미지·`og-image.png` 등)과 `sitemap.xml`·`robots.txt`는 변경 영향이 명확할 때만 손댑니다.

## 커밋 메시지 규칙

```
<type>: <short description>
```

| type | 용도 |
|---|---|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 리팩토링 |
| `docs` | 문서 수정 |
| `chore` | 설정, 의존성 등 |

## 검증

- **데이터·스크립트 변경**: `node -c build.js`(구문) → `node build.js` 실행 후 **재실행 결과가
  동일한지(idempotent)** 확인합니다. `projects.json`은 JSON 파싱, `sitemap.xml`은 XML 파싱으로
  유효성을 점검합니다.
- **JS 변경**: `node -c script.js` 로 구문을 확인합니다.
- **레이아웃·스타일 변경**: 가능한 범위에서 브라우저로 렌더를 확인하고(다크 모드·EN 토글 포함),
  직접 확인이 어려운 부분은 사용자에게 확인 요청 사항으로 남깁니다.
- **문서만 변경한 경우**에도 링크와 명령 예시를 직접 확인합니다.
- PowerShell에서 한글 문서를 읽을 때는 `Get-Content -Encoding UTF8`을 사용합니다.
