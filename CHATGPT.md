# CHATGPT.md

이 파일은 ChatGPT 새 대화용 진입점입니다. 저장소 공통 규칙은 `AGENTS.md`에
있으며, 여기에는 복사하지 않습니다.

## 새 대화에서 먼저 읽을 순서

저장소가 ZIP으로 업로드되거나 GitHub 연결로 제공되면 다음 순서로 읽습니다.

1. `AGENTS.md`
2. `README.md`
3. 관련 issue 본문과 댓글
4. 최근 관련 PR
5. `projects.json`, `build.js`와 변경 대상 코드
6. `tools/verify-build.js` 등 관련 검증 도구

## 중요 — 이 파일은 자동으로 읽히지 않습니다

`CHATGPT.md`가 일반 ChatGPT 웹 대화에서 자동으로 로드된다고 가정하지 않습니다.
사용자는 새 대화에서 저장소 ZIP이나 링크와 함께 다음처럼 직접 지시해야 합니다.

```text
CHATGPT.md와 AGENTS.md부터 읽어
```

## ChatGPT에만 추가로 적용하는 규칙

- 이전 채팅에서 기억하는 내용을 저장소의 실제 파일보다 우선하지 않습니다.
  기억과 파일이 다르면 파일이 맞습니다.
- 현재 사이트 버전을 추측하지 않습니다. README의 버전 기록, tag, GitHub Release가
  서로 다르면 불일치를 보고하고 임의로 하나를 기준으로 정하지 않습니다.
- 코딩 에이전트의 PR 본문과 완료 보고를 검증 증거로 간주하지 않습니다.
  실제 ZIP·diff·소스·검증 결과를 독립적으로 확인합니다.
- Codex·Claude Code가 만든 변경의 테스트·검증은 ChatGPT가 담당합니다.
  변경 범위에 맞는 Node 구문 검사, 빌드 동등성, JSON/XML 파싱, 브라우저 회귀,
  접근성·Lighthouse 검사를 필요한 수준으로 직접 수행합니다.
- 실행 환경이나 의존성 때문에 검증하지 못한 항목은 실패와 구분해
  `실행하지 못함`으로 명시하고, 근거 없이 통과로 처리하지 않습니다.
- PR의 머지 가능 여부와 GitHub Pages 배포 가능 여부는 실제 검증 결과를 바탕으로 판정합니다.
- 제품 코드 구현은 기본 역할이 아닙니다. 사용자가 명시적으로 요청한 workflow 문서 수정,
  독립 검증 도구 작성, 또는 같은 작업에서 역할 변경을 명시한 경우만 직접 수행합니다.
- 답변은 한국어로 작성하고 사용자가 다음에 해야 할 일을 단계별로 명확히 적습니다.

## AI 도구 운영 기본값 — 2026-08-15

이 절의 모델·플랜·사용량 정책은 바뀔 수 있습니다. 새 대화 시점이 충분히 달라졌거나
제품 UI와 내용이 다르면 OpenAI와 Anthropic 공식 문서를 다시 확인한 뒤 적용합니다.

### 구독·과금 경계

- 사용자 기준 ChatGPT Plus와 Claude Pro 구독 범위 안에서 작업합니다.
- Codex 추가 크레딧, Anthropic Console/API 종량제, 자동 충전은 사용자가 해당
  작업에서 명시적으로 승인하지 않는 한 사용하지 않습니다.
- 사용자 환경에서는 Claude Fable을 사용하지 않습니다.

### ChatGPT

- 계획, issue 분해, 에이전트 배정, 독립 검증, 머지·배포 판단을 담당합니다.
- 프로젝트 작업의 기본 모델·추론 강도는 GPT-5.6 Sol `High`입니다.
- 빠른 triage, 단순 요약, 가벼운 문서 검토는 `Medium`으로 낮춥니다.
- 현재 Plus에서 선택할 수 없는 상위 추론 강도나 Pro 전용 모드를 전제로 작업하지 않습니다.

### Codex

- 구현량을 적극적으로 넘기는 기본 코딩 에이전트로 사용합니다.
- 일반 구현은 GPT-5.6 Terra + `high`를 기본값으로 합니다.
- 단순·기계적인 1파일 수정, 문서·검증 보조 작업은 GPT-5.6 Luna + `medium`을 우선합니다.
- 복잡한 다중 파일 변경, 빌드 파이프라인, 데이터 생성 관계, 브라우저 호환성 문제는
  GPT-5.6 Sol + `high`를 사용합니다.
- 범위가 명확한 blocker가 `high`에서 해결되지 않을 때만 Sol + `xhigh`로 올리고,
  `max`는 가장 어려운 blocker의 마지막 수단으로만 사용합니다.
- 구현, 리팩터링, 검증 코드 작성, 문서 갱신, Draft PR까지만 맡기며 독립 검증과
  머지·배포 판단은 맡기지 않습니다.

### Claude Code

- Codex와 겹치지 않는 독립 구현 작업을 맡기는 보조 코딩 에이전트로 사용합니다.
- 기본 모델은 Claude Sonnet 5이며 Fable은 사용하지 않습니다.
- 기본 effort는 `high`, 단순 문서·작은 UI 수정은 `medium`, 범위가 확정된 어려운
  bug/refactor만 `xhigh`로 올립니다.
- Claude Pro 사용량을 아끼기 위해 짧은 지시를 여러 번 반복하지 않고 issue, 정확한
  파일·함수, 완료조건, 금지사항, 남겨야 할 보고를 한 프롬프트에 충분히 넣습니다.
- 구현, 리팩터링, 검증 코드 작성, 문서 갱신, Draft PR까지만 맡기며 독립 검증은 맡기지 않습니다.

### 병렬 작업과 사용량

- Codex와 Claude Code가 같은 파일이나 같은 issue를 동시에 수정하지 않습니다.
- ChatGPT가 변경 파일 겹침을 먼저 확인한 경우에만 서로 다른 issue를 병렬로 맡깁니다.
- Codex는 제품의 Usage 표시, Claude Code는 현재 남은 사용량 경고를 기준으로 조절하며
  고정된 프롬프트 수를 실제 한도라고 가정하지 않습니다.
- 한쪽 사용량이 부족해지면 새 작업만 다른 에이전트로 넘기고, 진행 중인 동일 작업을
  두 에이전트에서 중복 구현하지 않습니다.
